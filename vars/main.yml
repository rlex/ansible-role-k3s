k3s_initial_master: "{{ inventory_hostname == hostvars[groups[k3s_master_group][0]]['inventory_hostname'] }}"
k3s_arch_table:
  aarch64:
    arch: arm64
    suffix: "-arm64"
  x86_64:
    arch: amd64
    prefix: ""
